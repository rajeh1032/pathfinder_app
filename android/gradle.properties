org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Disable Kotlin incremental compilation to avoid the Windows "different roots"
# crash when the Flutter pub cache (C:) and the project (F:) live on different drives.
kotlin.incremental=false
org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-17.0.11.9-hotspot